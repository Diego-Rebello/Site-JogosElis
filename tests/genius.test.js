import { describe,expect,it } from 'vitest';import { ampliarSequencia,conferirJogada,criarGerador,estrelasPorRecorde } from '../Games/genius/jogo.js';
describe('Genius das Cores',()=>{it('repete a sequência quando recebe a mesma semente',()=>{const a=criarGerador(12345),b=criarGerador(12345);let sa=[],sb=[];for(let i=0;i<30;i++){sa=ampliarSequencia(sa,a);sb=ampliarSequencia(sb,b)}expect(sa).toEqual(sb);expect(new Set(sa).size).toBeGreaterThan(1)});it('confere cada passo',()=>{expect(conferirJogada(['rosa','azul'],['rosa'])).toBe(true);expect(conferirJogada(['rosa','azul'],['rosa','verde'])).toBe(false)});it('calcula estrelas em 5, 8 e 12',()=>{expect([4,5,8,12].map(estrelasPorRecorde)).toEqual([0,1,2,3])})});
